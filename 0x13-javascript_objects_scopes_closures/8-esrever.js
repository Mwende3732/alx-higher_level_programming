#!/usr/bin/node
exports.esrever = function (list) {
e ((len - i) > 0) {
    const aux = list[len];
     let len = list.length - 1;
  let i = 0;
  whil list[len] = list[i];
    list[i] = aux;
    i++;
    len--;
  }
  return list;
};
