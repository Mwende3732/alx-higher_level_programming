#!/usr/bin/node
const SquareP = require('./5-square');

class Square extends SquareP {
h; j++) {
        s += c;
      }
      console.lo  charPrint (c) {
    if (c === undefined) {
      c = 'X';
    }
    for (let i = 0; i < this.height; i++) {
      let s = '';
      for (let j = 0; j < this.widtg(s);
    }
  }
}

module.exports = Square;
